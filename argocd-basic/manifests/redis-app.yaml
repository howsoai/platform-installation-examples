apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-redis 
  namespace: argocd
spec:
  project: howso-platform 
  source:
    repoURL: registry.how.so/howso-platform/dev-1 
    targetRevision: 18.4.0 
    chart: redis 
    helm:
      values: |
        {}
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd 
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
